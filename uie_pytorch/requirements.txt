# -*- coding: utf-8 -*-
numpy >=1.22
six
colorlog
torch >=1.10 # cpu版，允许 2.x 版本以兼容最新 Python/镜像
transformers >=4.18,<5.0
packaging
tqdm
sentencepiece
protobuf==3.19.0
onnxruntime # cpu版

# faster-tokenizer==0.2.0 # faster-tokenizer 已经废弃
# fast-tokenizer-python==1.0.0  # 可选加速依赖，当前环境下通常无法从 PyPI 安装（无可用发行版）

# paddlepaddle # paddlepaddle 是可选的
